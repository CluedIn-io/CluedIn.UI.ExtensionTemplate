{
  "name": "MyApplicationName",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@cluedin/ui": "3.3.0-next-02",
    "react": "^16.9.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "react-scripts": "3.4.2"
  },
  "scripts": {
    "prepare": "node ./node_modules/@cluedin/ui/src/prepare",
    "start": "npm run prepare && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
